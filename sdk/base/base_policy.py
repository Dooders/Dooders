from abc import ABC, abstractmethod
from typing import Any

class BasePolicy(ABC):
    """ 
    """

    @abstractmethod
    def execute(self, dooder) -> Any:
      
      raise NotImplementedError
       
